import { TradeName } from "./TradeName";

/**
 * AuxiliaryTradeNames
 * @targetNSAlias `tns`
 * @targetNamespace `http://bis/dataservices/companyquery/v1`
 */
export interface AuxiliaryTradeNames {
    /** TradeName[] */
    TradeName?: Array<TradeName>;
}
